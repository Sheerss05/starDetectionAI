[project]
name = "stardetectionai"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
description = "Star AI Streamlit deployment metadata"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
